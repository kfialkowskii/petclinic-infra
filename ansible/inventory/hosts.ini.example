; Skopiuj do hosts.ini i uzupełnij IP z "terraform output vm_public_ip"
; hosts.ini jest w .gitignore — nie trafi do repo

[app]
VM_PUBLIC_IP ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/petclinic
